name = "whoisunder-worker"
main = "worker.js"
compatibility_date = "2025-12-12"

# 替换为你的 KV Namespace ID
# kv_namespaces = [
#  { binding = "GAMES_KV", id = "<REPLACE_WITH_KV_NAMESPACE_ID>" }
#]

# KV 命名空间绑定
# 请使用 wrangler kv:namespace create "GAMES_KV" 创建你自己的 KV 命名空间
# 然后将生成的 ID 替换下面的示例值
[[kv_namespaces]]
binding = "GAMES_KV"
id = "your_kv_namespace_id_here"          # 替换为你的生产环境 KV ID
preview_id = "your_preview_kv_id_here"    # 替换为你的预览环境 KV ID